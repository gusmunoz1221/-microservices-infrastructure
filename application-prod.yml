spring:
  application:
    name: config-product
eureka:
  instance:
    prefer-ip-address: true
  client:
    fetch-registry: true
    register-with-eureka: tue
    service-url:
      defaultZone: http://localhost:8761/eureka
